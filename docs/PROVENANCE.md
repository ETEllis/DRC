# Provenance and publication state

**Current repository:** `ETEllis/DRC` (public). **Pages source:** `main` → `/ (root)`. **Front-facing URL:** https://etellis.github.io/DRC/.

## Source lineages

1. The local September 3 handoff supplied two author manuscripts as TeX and PDF. Their original bytes remain in `sources/original/`; the SHA-256 register is `sources/SHA256SUMS`.
2. On September 25, the author supplied `Developmental_Regulatory_Cascade.html` as a file preview. Its embedded `research-source.zip` contained the third named author draft, a full integrated manuscript, figures, evidence data, code, and a 22-page PDF. The **unchanged preview and archive** are in `sources/received/`; `sources/received/SHA256SUMS` records their hashes. The archive's own `docs/SOURCE_MANIFEST.json` records the three included source-document hashes.
3. The preview's source package was imported as the principal perinatal and navigational foundation for this release. `papers/perinatal-extension-source.md` and the supplied source PDFs remain byte-identical to the received archive. `papers/integrated-paper.md`, `papers/latex/`, `data/`, `src/`, `index.html`, diagrams, and the current two-column PDF are derivative, versioned work.
4. DRC's first public implementation was committed as `06000154b9e31d675ead72ecbcde1c10fe5b1143`. Its light atlas, shorter paper, and source ledger are preserved under `archive/first-atlas/`. The current site uses its editorial reading direction and distinct later-life evidence; it does not imply that the first implementation contained the then-unavailable perinatal draft.

The received preview's original `docs/AUTHORITY.md` correctly described **its own** ChatGPT Work environment as lacking GitHub creation capability. That historical statement remains in the untouched archive. It does not describe the current Mac checkout: this repository and GitHub Pages site have since been created and verified live.

## Claim boundary

The source package's strongest new material concerns birth signaling, maternal strain inheritance, *L. reuteri* in mouse social circuits, α₂δ synaptogenesis and release, and competing explanations for developmental outcomes. The earlier DRC release's distinct material concerns mixed human stress-epigenetic findings, task event rate, autonomic heterogeneity, masking, chronotype, and the individual/collective AI creativity contrast. [INTEGRATION_CROSSWALK.md](INTEGRATION_CROSSWALK.md) records their placement in the unified site and paper.

Neither the supplied source archive nor this release includes the full historical project chat. Its directives are not treated as independent evidence. A linked paper may establish a bounded component without validating a downstream bridge.
